---
"fingerprint-pro-server-api-php-sdk": minor
---

Limit `VisitorsGetResponse.visits` to a maximum of one item and clarify that `GET /visitors/{visitor_id}` is deprecated in favor of `GET /events/search`.
